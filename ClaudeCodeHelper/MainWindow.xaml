<Window x:Class="ClaudeCodeHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClaudeCodeHelper"
        mc:Ignorable="d"
        Title="ClaudeCodeHelper" Height="320" Width="720" MinHeight="240" MinWidth="520">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row0: 경로 입력 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="경로:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox   Grid.Column="1" x:Name="txtPath" VerticalContentAlignment="Center" Height="26"/>
            <Button    Grid.Column="2" x:Name="btnBrowse" Content="찾아보기" Click="BtnBrowse_Click" Margin="6,0,0,0" Padding="12,0" Height="26"/>
            <Button    Grid.Column="3" x:Name="btnRun"    Content="실행"     Click="BtnRun_Click"    Margin="6,0,0,0" Padding="16,0" Height="26"/>
        </Grid>

        <!-- Row1: 모든 권한으로 실행 토글 -->
        <CheckBox Grid.Row="1" x:Name="chkFullPermission"
                  Content="모든 권한으로 실행 (--dangerously-skip-permissions)"
                  Checked="ChkFullPermission_Changed" Unchecked="ChkFullPermission_Changed"
                  Margin="0,10,0,0"/>

        <!-- Row2: 목록 라벨 -->
        <TextBlock Grid.Row="2" Text="저장된 경로 목록:" Margin="0,12,0,4"/>

        <!-- Row3: 목록 + 버튼 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListBox Grid.Column="0" x:Name="lstPaths" SelectionChanged="LstPaths_SelectionChanged"/>
            <StackPanel Grid.Column="1" Margin="8,0,0,0" Width="90">
                <Button x:Name="btnDelete"     Content="삭제"      Click="BtnDelete_Click"     Margin="0,0,0,6" Height="28"/>
                <Button x:Name="btnClearAll"   Content="전체 삭제"  Click="BtnClearAll_Click"   Margin="0,0,0,6" Height="28"/>
                <Button x:Name="btnOpenFolder" Content="폴더 열기"  Click="BtnOpenFolder_Click" Height="28"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
