<Window x:Class="ClaudeCodeHelper.McpPresetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClaudeCodeHelper"
        mc:Ignorable="d"
        Title="MCP 프리셋" Height="360" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="이름 (claude mcp add의 name)" Margin="0,0,0,2"/>
        <TextBox   Grid.Row="1" x:Name="txtName" Height="26" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Text="명령 (예: npx, python)" Margin="0,0,0,2"/>
        <TextBox   Grid.Row="3" x:Name="txtCommand" Height="26" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="4" Text="인자 (한 줄에 하나씩, -- 뒤에 붙음)"
                   Margin="0,0,0,2" VerticalAlignment="Top"/>
        <TextBox   Grid.Row="4" x:Name="txtArgs" Margin="0,20,0,8"
                   AcceptsReturn="True" TextWrapping="Wrap"
                   VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOk"     Content="확인" Width="80" Height="28" Margin="0,0,8,0" Click="BtnOk_Click"/>
            <Button x:Name="btnCancel" Content="취소" Width="80" Height="28" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
